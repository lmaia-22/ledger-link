---
phase: 01-foundation-and-sage-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.server.json
  - client/tsconfig.json
  - client/vite.config.ts
  - client/index.html
  - drizzle.config.ts
  - vitest.config.ts
  - src/types/domain.ts
  - src/sage-bridge/types.ts
  - src/db/schema.ts
  - src/db/client.ts
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - SAGE-01
  - SAGE-02
  - SAGE-04

must_haves:
  truths:
    - "Project compiles with tsc --noEmit without errors"
    - "Vitest runs and passes with zero tests (no config errors)"
    - "Database schema defines companies and transactions tables with integer pence amounts"
    - "Shared domain types exist for SageCompany, SageTransaction, and bridge IPC messages"
  artifacts:
    - path: "package.json"
      provides: "Monorepo dependencies for backend and frontend"
      contains: "fastify"
    - path: "src/db/schema.ts"
      provides: "Drizzle table definitions with integer pence"
      contains: "amountPence"
    - path: "src/types/domain.ts"
      provides: "Shared domain types"
      exports: ["SageCompany", "SageTransaction"]
    - path: "src/sage-bridge/types.ts"
      provides: "Bridge IPC message types"
      exports: ["BridgeRequest", "BridgeResponse"]
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
      contains: "defineConfig"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/types/domain.ts"
      via: "integer pence convention"
      pattern: "integer.*amount_pence"
---

<objective>
Scaffold the greenfield project with all dependencies, TypeScript configuration, database schema, shared types, and test infrastructure.

Purpose: Establish the foundational project structure that all subsequent plans build on. Every file path, type definition, and configuration choice made here becomes the contract for Plans 02-04.
Output: A compilable TypeScript project with Drizzle schema, domain types, bridge IPC types, and Vitest ready to run.
</objective>

<execution_context>
@/Users/luismaia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismaia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-sage-bridge/01-CONTEXT.md
@.planning/phases/01-foundation-and-sage-bridge/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with all dependencies and TypeScript config</name>
  <files>
    package.json
    tsconfig.json
    tsconfig.server.json
    client/tsconfig.json
    client/vite.config.ts
    client/index.html
    client/src/main.tsx
    drizzle.config.ts
    vitest.config.ts
    .env.example
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation-and-sage-bridge/01-RESEARCH.md
  </read_first>
  <action>
Create the project from scratch with the following exact structure and contents:

**package.json** — Root package with these exact dependencies:
- dependencies: `fastify@^5`, `@fastify/static@^8`, `better-sqlite3@^11`, `drizzle-orm@^0.40`, `zod@^3`, `fastify-type-provider-zod@^4`
- dependencies: `react@^19`, `react-dom@^19`, `@tanstack/react-query@^5`, `@tanstack/react-router@^1`, `@tanstack/react-table@^8`, `lucide-react@^0.468`
- devDependencies: `typescript@^5`, `vite@^6`, `@vitejs/plugin-react@^4`, `@tanstack/router-plugin@^1`, `drizzle-kit@^0.30`, `@types/better-sqlite3@^7`, `tsx@^4`, `vitest@^2`, `tailwindcss@^3`, `postcss@^8`, `autoprefixer@^10`, `@types/react@^19`, `@types/react-dom@^19`
- scripts: `"dev:server": "tsx watch src/index.ts"`, `"dev:client": "vite --config client/vite.config.ts"`, `"build:client": "vite build --config client/vite.config.ts"`, `"test": "vitest run"`, `"test:watch": "vitest"`, `"db:push": "drizzle-kit push"`, `"db:studio": "drizzle-kit studio"`

NOTE: Do NOT include `winax` in dependencies — it only works on Windows and will fail npm install on macOS/Linux. It will be conditionally loaded by the bridge-host at runtime on Windows only. Add a comment in package.json or .env.example noting this.

**tsconfig.json** — Root config for backend:
- `target: "ES2022"`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `strict: true`, `esModuleInterop: true`, `skipLibCheck: true`
- `outDir: "./dist"`, `rootDir: "./src"`, `include: ["src/**/*"]`

**tsconfig.server.json** — Extends root tsconfig, backend only.

**client/tsconfig.json** — Frontend config:
- Extends `@vitejs/plugin-react` defaults, `jsx: "react-jsx"`, paths alias `@/*` to `./src/*`

**client/vite.config.ts** — Vite configuration:
- Plugins: `tanstackRouter({ target: 'react', autoCodeSplitting: true })` BEFORE `react()`
- `server.proxy`: `{ '/api': 'http://localhost:3000' }`
- `resolve.alias`: `@` mapped to `./src`
- `build.outDir`: `dist`

**client/index.html** — Minimal HTML with `<html lang="pt">`, div#root, script src `/src/main.tsx`

**client/src/main.tsx** — Minimal placeholder: `ReactDOM.createRoot(document.getElementById('root')!).render(<div>Ledger Link</div>);` (full setup comes in Plan 03)

**drizzle.config.ts**:
```typescript
import { defineConfig } from 'drizzle-kit';
export default defineConfig({
  dialect: 'sqlite',
  schema: './src/db/schema.ts',
  out: './drizzle',
  dbCredentials: { url: process.env.DB_PATH ?? './data/ledger-link.db' },
});
```

**vitest.config.ts**:
```typescript
import { defineConfig } from 'vitest/config';
import path from 'node:path';
export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

**.env.example**:
```
DB_PATH=./data/ledger-link.db
PORT=3000
HOST=0.0.0.0
# winax is required on the Windows deployment machine only.
# Run: npm install winax --save on the target Windows machine.
```

**.gitignore**: node_modules, dist, client/dist, data/, drizzle/, *.db, .env
  </action>
  <verify>
    <automated>cd /Users/luismaia/Developer/personal/ledger-link && npm install && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "fastify" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "drizzle-orm" in dependencies
    - package.json contains "@tanstack/react-router" in dependencies
    - package.json does NOT contain "winax"
    - tsconfig.json contains "NodeNext" for moduleResolution
    - client/vite.config.ts contains "tanstackRouter" before "react()"
    - client/vite.config.ts contains "proxy" with "/api"
    - vitest.config.ts contains "defineConfig"
    - .env.example contains "DB_PATH"
    - .gitignore contains "node_modules"
    - client/index.html contains 'lang="pt"'
  </acceptance_criteria>
  <done>npm install succeeds, tsc --noEmit passes for backend tsconfig, all config files exist with correct contents</done>
</task>

<task type="auto">
  <name>Task 2: Create shared domain types, bridge IPC types, and database schema</name>
  <files>
    src/types/domain.ts
    src/sage-bridge/types.ts
    src/db/schema.ts
    src/db/client.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-and-sage-bridge/01-RESEARCH.md
    package.json
  </read_first>
  <action>
Create the type system and database schema that all subsequent plans depend on.

**src/types/domain.ts** — Shared domain types:
```typescript
export interface SageCompany {
  id: number;
  sagePath: string;
  name: string;
  isConnected: boolean;
  connectedAt: Date | null;
}

export interface SageTransaction {
  id: number;
  companyId: number;
  sageTxRef: string;
  txDate: Date;
  reference: string | null;
  description: string | null;
  amountPence: number; // CRITICAL: integer pence, never float
  txType: string | null;
  importedAt: Date;
}

export interface PaginatedResponse<T> {
  rows: T[];
  total: number;
  page: number;
  pageSize: number;
}

export interface TransactionFilters {
  page?: number;
  pageSize?: number;
  from?: string;
  to?: string;
  search?: string;
  sortBy?: 'txDate' | 'reference' | 'description' | 'amountPence' | 'txType';
  sortOrder?: 'asc' | 'desc';
}
```

**src/sage-bridge/types.ts** — Bridge IPC discriminated union types:
```typescript
import type { SageCompany, SageTransaction, TransactionFilters } from '../types/domain.js';

export type BridgeRequest =
  | { id: string; type: 'PING' }
  | { id: string; type: 'LIST_COMPANIES' }
  | { id: string; type: 'READ_TRANSACTIONS'; companyPath: string; filters: TransactionFilters };

export type BridgeResponse =
  | { id: string; ok: true; type: 'PONG' }
  | { id: string; ok: true; type: 'COMPANIES'; data: SageCompany[] }
  | { id: string; ok: true; type: 'TRANSACTIONS'; data: SageTransaction[]; total: number }
  | { id: string; ok: false; error: string; code?: string };
```

**src/db/schema.ts** — Drizzle SQLite schema with integer pence:
```typescript
import { sqliteTable, integer, text } from 'drizzle-orm/sqlite-core';

export const companies = sqliteTable('companies', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  sagePath: text('sage_path').notNull().unique(),
  name: text('name').notNull(),
  isConnected: integer('is_connected', { mode: 'boolean' }).notNull().default(false),
  connectedAt: integer('connected_at', { mode: 'timestamp' }),
});

export const transactions = sqliteTable('transactions', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  companyId: integer('company_id').notNull().references(() => companies.id),
  sageTxRef: text('sage_tx_ref').notNull(),
  txDate: integer('tx_date', { mode: 'timestamp' }).notNull(),
  reference: text('reference'),
  description: text('description'),
  amountPence: integer('amount_pence').notNull(),
  txType: text('tx_type'),
  importedAt: integer('imported_at', { mode: 'timestamp' }).notNull(),
});
```

**src/db/client.ts** — Database singleton:
```typescript
import Database from 'better-sqlite3';
import { drizzle } from 'drizzle-orm/better-sqlite3';
import * as schema from './schema.js';
import { mkdirSync } from 'node:fs';
import { dirname } from 'node:path';

const dbPath = process.env.DB_PATH ?? './data/ledger-link.db';
mkdirSync(dirname(dbPath), { recursive: true });

const sqlite = new Database(dbPath);
sqlite.pragma('journal_mode = WAL');

export const db = drizzle(sqlite, { schema });
```
  </action>
  <verify>
    <automated>cd /Users/luismaia/Developer/personal/ledger-link && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types/domain.ts contains "export interface SageCompany"
    - src/types/domain.ts contains "export interface SageTransaction"
    - src/types/domain.ts contains "amountPence: number"
    - src/types/domain.ts contains "export interface PaginatedResponse"
    - src/types/domain.ts contains "export interface TransactionFilters"
    - src/sage-bridge/types.ts contains "export type BridgeRequest"
    - src/sage-bridge/types.ts contains "export type BridgeResponse"
    - src/sage-bridge/types.ts contains "type: 'PING'"
    - src/sage-bridge/types.ts contains "type: 'LIST_COMPANIES'"
    - src/sage-bridge/types.ts contains "type: 'READ_TRANSACTIONS'"
    - src/db/schema.ts contains "amount_pence"
    - src/db/schema.ts contains "integer('amount_pence')"
    - src/db/schema.ts does NOT contain "real(" or "REAL"
    - src/db/client.ts contains "journal_mode = WAL"
    - src/db/client.ts contains "drizzle(sqlite, { schema })"
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All types compile, schema uses INTEGER for amounts (not REAL), bridge types use discriminated unions, tsc --noEmit passes</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffolds for all phase requirements</name>
  <files>
    tests/utils/format-amount.test.ts
    tests/sage-bridge/pence-conversion.test.ts
    tests/sage-bridge/bridge-client.test.ts
    tests/api/transactions.test.ts
    tests/server/spa-fallback.test.ts
    client/src/lib/utils.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-and-sage-bridge/01-RESEARCH.md
    vitest.config.ts
    src/types/domain.ts
  </read_first>
  <action>
Create the test scaffolds identified in RESEARCH.md's Validation Architecture section, plus the `formatAmount` utility.

**client/src/lib/utils.ts** — shadcn cn() + formatAmount:
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

export function formatAmount(pence: number): string {
  return new Intl.NumberFormat('pt-PT', {
    style: 'currency',
    currency: 'EUR',
  }).format(pence / 100);
}
```

NOTE: Also add `clsx` and `tailwind-merge` to package.json dependencies: `npm install clsx tailwind-merge`

**tests/utils/format-amount.test.ts**:
- Test `formatAmount(123456)` returns string containing `1.234,56` and `€`
- Test `formatAmount(0)` returns string containing `0,00` and `€`
- Test `formatAmount(100)` returns string containing `1,00` and `€`
- Test `formatAmount(-50075)` returns string containing `-500,75` and `€`
- Test `formatAmount(1)` returns string containing `0,01` and `€`

**tests/sage-bridge/pence-conversion.test.ts**:
- Write a pure function `toPence(floatAmount: number): number` that does `Math.round(floatAmount * 100)`
- Export it from a small utility file `src/sage-bridge/pence.ts`
- Test: `toPence(115.10)` equals `11510`
- Test: `toPence(0.01)` equals `1`
- Test: `toPence(1234.56)` equals `123456`
- Test: `toPence(0.1 + 0.2)` equals `30` (IEEE 754 edge case)
- Test: result is always an integer: `Number.isInteger(toPence(99.99))` is true

**tests/sage-bridge/bridge-client.test.ts**:
- Placeholder test file with a single passing test: `test('bridge-client module exists', () => { expect(true).toBe(true); })`
- Comment: `// Full IPC tests added in Plan 02 when bridge-client.ts is implemented`

**tests/api/transactions.test.ts**:
- Placeholder test file with a single passing test: `test('transactions API shape', () => { expect(true).toBe(true); })`
- Comment: `// Full API tests added in Plan 02 when Fastify routes are implemented`

**tests/server/spa-fallback.test.ts**:
- Placeholder test file with a single passing test: `test('SPA fallback', () => { expect(true).toBe(true); })`
- Comment: `// Full SPA fallback tests added in Plan 02 when Fastify server is implemented`

Create `src/sage-bridge/pence.ts`:
```typescript
export function toPence(floatAmount: number): number {
  return Math.round(floatAmount * 100);
}
```
  </action>
  <verify>
    <automated>cd /Users/luismaia/Developer/personal/ledger-link && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/utils/format-amount.test.ts contains "formatAmount(123456)"
    - tests/sage-bridge/pence-conversion.test.ts contains "toPence(115.10)" and "11510"
    - tests/sage-bridge/pence-conversion.test.ts contains "0.1 + 0.2"
    - src/sage-bridge/pence.ts contains "Math.round(floatAmount * 100)"
    - client/src/lib/utils.ts contains "formatAmount"
    - client/src/lib/utils.ts contains "pt-PT"
    - client/src/lib/utils.ts contains "pence / 100"
    - npx vitest run exits 0 with all tests passing
  </acceptance_criteria>
  <done>All Vitest tests pass (format-amount, pence-conversion, and placeholder tests), formatAmount utility produces Portuguese locale output, toPence handles IEEE 754 edge cases</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for backend
- `npx vitest run` passes all tests
- `src/db/schema.ts` uses only `integer()` for amount columns (never `real()`)
- All exported types compile without errors
</verification>

<success_criteria>
- Project installs and compiles on macOS/Linux (no Windows-only deps in package.json)
- Database schema enforces integer-pence convention
- Bridge IPC types define the full discriminated union contract
- Vitest runs with passing format-amount and pence-conversion tests
- All config files (vite, drizzle, vitest, tsconfig) are correct and functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-sage-bridge/01-01-SUMMARY.md`
</output>
